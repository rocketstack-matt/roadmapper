# GitHub Personal Access Token (optional)
# Without this token: 60 requests/hour
# With this token: 5,000 requests/hour
#
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "Roadmapper"
# 4. Select scope: "public_repo" (read access to public repositories)
# 5. Copy the token and paste it below
#
# GITHUB_TOKEN=your_token_here

# Upstash Redis (required for API key verification, rate limiting, and caching)
# Without these: service runs unrestricted (no auth, no rate limits, no caching)
# With these: repos must register and add a .roadmapper file to use the service
#
# To get credentials:
# 1. Sign up at https://upstash.com
# 2. Create a new Redis database
# 3. Copy the REST URL and token from the database details page
#
# UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_token_here

# Resend (optional, for email confirmation on registration)
# Without this: keys work immediately after registration (no email confirmation)
# With this: registration requires email confirmation before the key activates
#
# To get an API key:
# 1. Sign up at https://resend.com
# 2. Verify your sending domain
# 3. Create an API key in Settings > API Keys
#
# RESEND_API_KEY=re_your_key_here
# FROM_EMAIL=Roadmapper <noreply@yourdomain.com>
